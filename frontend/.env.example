# Скопируйте в .env или задайте через UI Vercel (Settings -> Environment Variables).
# Без него фронт будет ходить в "/api" относительно своего домена (417 в проде).
VITE_API_URL=https://cafe-backend.up.railway.app